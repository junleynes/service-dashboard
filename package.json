{
  "name": "service-dashboard-backend",
  "version": "1.0.0",
  "description": "Backend server for the service dashboard.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "esbuild index.tsx --bundle --outfile=bundle.js --minify"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "nodemon": "^3.1.0"
  }
}